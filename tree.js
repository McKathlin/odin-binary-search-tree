export default (function() {
  class Tree {
    constructor(arr) {
      this._root = this.buildTree(arr);
    }
    
    buildTree(arr) {
      // TODO
    }

    // TODO
  }

  class Node {
    constructor(value) {
      this.value = value;
      this.left = null;
      this.right = null;
    }

    // TODO: Anything else needed here?
  }
}());